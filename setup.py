from distutils.core import setup
setup(
       name = "transcode" ,
       version = "0.10" ,
       description = "this is transcode" ,
       packages = ['transcode'],
       package_dir={'transcode':'../transcode'},
      

)